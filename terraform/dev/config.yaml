environment: dev
account_id: "REPLACE-ME"
region: ap-southeast-1

services:
  api:
    image: api:REPLACE-ME
    size: small
    public: true
    health_check: /healthz

database:
  enabled: true
  size: small
